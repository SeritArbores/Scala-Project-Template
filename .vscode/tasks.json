{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "sbt: compile",
            "type": "shell",
            "command": "sbt",
            "args": [
                "compile"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
